<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_esg_report_builder_form" model="ir.ui.view">
        <field name="name">esg.report.builder.form</field>
        <field name="model">esg.report.builder</field>
        <field name="arch" type="xml">
            <form string="ESG Report Builder">
                <sheet>
                    <div class="oe_title">
                        <h1>ESG Report Builder</h1>
                        <p class="text-muted">Select filters and generate your ESG report.</p>
                    </div>
                    <group>
                        <group string="Date Range &amp; Scope">
                            <field name="date_from"/>
                            <field name="date_to"/>
                            <field name="module_type"/>
                            <field name="report_type"/>
                            <field name="export_format"/>
                        </group>
                        <group string="Filter By">
                            <field name="department_ids" widget="many2many_tags"/>
                            <field name="employee_ids" widget="many2many_tags"/>
                            <field name="challenge_ids" widget="many2many_tags"/>
                            <field name="category_ids" widget="many2many_tags"/>
                        </group>
                    </group>
                </sheet>
                <footer>
                    <button name="action_generate_report" string="Generate Report"
                            type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Wizard Action -->
    <record id="action_esg_report_builder_wizard" model="ir.actions.act_window">
        <field name="name">ESG Report Builder</field>
        <field name="res_model">esg.report.builder</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="view_id" ref="view_esg_report_builder_form"/>
    </record>
</odoo>
